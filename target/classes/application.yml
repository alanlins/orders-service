spring:
  application:
    name: orders-service
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI}
  kafka:
    bootstrap-servers: ${SPRING_KAFKA_BOOTSTRAP_SERVERS}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    properties:
      security.protocol: SASL_SSL
      ssl.truststore.location: file:/etc/ssl/certs/java/cacerts/client.truststore.jks
      ssl.truststore.password: ${TRUSTSTORE_PASSWORD}
      ssl.keystore.location: file:/etc/ssl/certs/java/cacerts/client.keystore.jks
      ssl.keystore.password: ${TRUSTSTORE_PASSWORD}
      ssl.key.password: ${TRUSTSTORE_PASSWORD}

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always
